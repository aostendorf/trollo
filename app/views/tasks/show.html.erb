<h1><%= @board.title %></h1>
<h2><%= @list.title %></h2> <h2><%@list.priority %></h2>
<h3><%= @task.title %></h3>
<%= link_to 'Edit Task', edit_board_list_path([@list, @task]) %>
<%= link_to 'Delete Task', [@list, @task], method: :delete %>
<%= link_to 'Back To Lists', board_lists_path(@board) %>